[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dailyfeed"
version = "0.1.0"
description = "Multi-agent personal daily feed aggregator"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["cmd*", "internal*", "api*"]
